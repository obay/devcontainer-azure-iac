# Azure IaC Development Container - Environment Variables
# Copy this file to .env and fill in your credentials
# cp .env.template .env

# =============================================================================
# Azure Resource Manager (Service Principal)
# =============================================================================
# Used for: az login, tofu init/plan/apply

ARM_TENANT_ID=<your-tenant-id>
ARM_SUBSCRIPTION_ID=<your-subscription-id>
ARM_CLIENT_ID=<service-principal-app-id>
ARM_CLIENT_SECRET=<service-principal-secret>

# =============================================================================
# Azure DevOps
# =============================================================================
# Used for: az devops commands, pipeline management

# Personal Access Token (PAT)
# Required scopes: Code (Read), Build (Read & Execute), Project (Read)
AZURE_DEVOPS_EXT_PAT=<your-personal-access-token>

# Organization URL
AZURE_DEVOPS_ORG_URL=<https://dev.azure.com/your-org>

# =============================================================================
# GitHub
# =============================================================================
# Used for: gh CLI commands, GitHub API access

# Personal Access Token (PAT)
# Required scopes: repo, read:org (adjust based on your needs)
GH_TOKEN=<your-github-personal-access-token>

# =============================================================================
# Optional: Additional Subscriptions
# =============================================================================
# Uncomment and fill in if working with multiple subscriptions

# ARM_CONNECTIVITY_SUBSCRIPTION_ID=<subscription-id>
# ARM_MANAGEMENT_SUBSCRIPTION_ID=<subscription-id>
